// src/shared/enums/part-request-status.enum.ts
export enum PartRequestStatus {
  REQUESTED = 'requested',
  READY = 'ready',
  DELIVERING = 'delivering',
  RECEIVED = 'received',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}
